on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

name: testthat-integration

jobs:
  testthat-module:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: windows-latest, r: 'release'}
          - {os: ubuntu-latest,  r: 'release'}
          #- {os: macOS-latest,   r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      GOOGLE_AUTH: ${{ secrets.GOOGLE_AUTH }}
      BRANCH_NAME: ${{ github.base_ref || github.ref_name }}
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true

    steps:

      - name: Check for Google Authorization
        if: ${{ env.GOOGLE_AUTH == '' }}
        run: exit 1

      - uses: actions/checkout@v4

      - uses: suz-estella/actions/setup-SpaDES-tests@main
        with:
           r-version: ${{ matrix.config.r }}

      - name: Run tests
        run: |
          Require::Require("googledrive")
          googledrive::drive_auth(path = Sys.getenv("GOOGLE_AUTH"))
          testthat::test_dir("tests/testthat", filter = "integration", stop_on_failure = TRUE)
        shell: Rscript {0}

